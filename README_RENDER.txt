ITZGET BOT — RENDER FREE (FIXED)

Render service type:
Web Service / Free

Build:
pip install -r requirements.txt

Start:
python bot.py

Environment Variables:
BOT_TOKEN=NEW_TOKEN_FROM_BOTFATHER
WEBAPP_URL=https://itzget-pvp.mrpowden.workers.dev
WEBHOOK_URL=https://itzgetbot.onrender.com/telegram/webhook
ADMIN_IDS=YOUR_TELEGRAM_ID

IMPORTANT:
- WEBAPP_URL is ONLY the Mini App URL.
- WEBHOOK_URL is the Render endpoint Telegram sends updates to.
- Do not upload .env or real tokens to GitHub.
- Revoke the previously exposed BOT_TOKEN and use a new one.
